[Unit]
Description=Mealie Receipt Manager
After=network.target

[Service]
Type=simple
Restart=always
WorkingDirectory={{ site_path }}
User={{ username }}
ExecStart={{ site_path }}/mealie.py mealie.app:app --port {{ port }} --host 127.0.0.1

[Install]
WantedBy=multi-user.target
